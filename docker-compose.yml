services:
  web:
    build: .
    container_name: bjj-detector
    ports:
      - "7860:5000"
    restart: unless-stopped
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5000
      - MODEL_PATH=best.pt
      - SECRET_KEY=bjj-detector-prod-change-me
